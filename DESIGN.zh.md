# Sesa — 多 Agent 议事引擎

> [English](DESIGN.md) · **中文**

> **Sesa** —— 芝麻开门。
> 让不同的模型开口，直到意见归一；开不了的那扇门，如实告诉你卡在哪。

把多个**已有的 agent CLI** 和**裸模型 API** 拉到同一张桌子上，按可选的议事协议辩论，
直到共识收敛——并且**如实报告分歧**，而不是假装统一。

---

## 1. 定位

### 它是什么

一个**无头议事引擎** + 三个前端（CLI / TUI / SDK）。核心职责只有一件事：

> 编排 N 个异质的 LLM 参与者围绕一个任务展开结构化讨论，测量他们的分歧，
> 在收敛或卡死时产出一份**带出处、带少数意见**的裁决。

### 与 AutoGen / CrewAI / ChatArena 的区别

| | 那些框架 | sesa |
|---|---|---|
| 编排对象 | 裸模型 API 上包一层 agent | **真实的 agent CLI**（claude code / codex / dsh / gemini-cli），自带工具与文件读写能力 |
| 讨论终止 | 固定轮数或 agent 自己说 "TERMINATE" | **可计算的分歧矩阵** + 稳定性检测 |
| 最终产出 | 一段合成文本 | 裁决书 + 分歧清单 + 少数意见 + 终局状态标签 |
| 裁判 | 通常是参赛者之一 | **不设裁判**：共识达成时共识本身即产出；未达成则如实并列，由人拍板 |
| 代码任务 | 靠模型自述"我测过了" | worktree 隔离 + **引擎亲自执行**的结果；且用交叉测试防"自己测自己" |

### 一个没有预期到的回报

"事件流是唯一真相来源"这条约束，最初是为了 replay 与复盘。实际用下来，
它带来了两件当初没想到的事：

1. **参与者能审我们的代码。** 一场议事中，Claude 引用我们自己落盘的
   `consensus.update` 事件，指出 `min_confidence` 聚合把未知参与者整体
   剔除而非按最差值计——一个我们自己没发现的 bug。它读的是产品的输出，
   不是源码。
2. **我们能审自己的结论。** `sesa eval` 一张表就暴露了「三场议事全部误报
   假共识」和「六份被中断的运行以零值污染均值」，而这两件事逐场去看
   都看不出来。

> 如实落盘的回报，不止于"出了问题能查"，而在于**让系统可以被自己和别人
> 检验**。这是把可观测性做成一等契约（而非日志）的真正理由。

### 非目标

- 不做通用 agent 框架（不提供工具调用、记忆、RAG——那是参与者自己的事）
- 不做模型网关（不管理 API key 轮换、限流、缓存）
- 不追求全自动无人值守（恰恰相反，人在环中是一等公民）

---

## 2. 核心抽象

```
Participant = Adapter(怎么调) × Model(哪个脑子) × Role(什么立场)
```

同一个 Adapter 可以配不同 Model，同一个 Model 可以配不同 Role。
"不同 agent + 不同 llm 的组合"就是这三元组的笛卡尔积。

### 2.1 分层

```
        ┌──────────────────────────────────────────────┐
        │  Engine  议事循环驱动（无 IO、无渲染）          │
        │  async generator，只吐 Event                  │
        └───────────────────┬──────────────────────────┘
                            │  同一份事件流
   ┌──────────┬─────────────┼──────────────┬────────────────┐
 CLI        TUI          SDK/库         MCP server      别人的产品
(--json)  (观战+干预)   (import)       (agent 调议会)   (消费 JSONL)
```

**关键约束：Engine 不认识终端。** TUI 只是事件流的第一个消费者，
和别人把它嵌进自己产品的地位完全平等。这是"方便二开"的唯一正确姿势。

### 2.2 四个可插拔层

| 层 | 接口 | 内置实现 |
|---|---|---|
| **Adapter** | `async stream(prompt, ctx) -> AsyncIterator[Delta]` | `openai_compat` / `anthropic` / `cli` |
| **Protocol** | `plan(state) -> list[Move]` | `ensemble` / `debate` / `council` / `adversarial` |
| **Consensus** | `assess(state) -> ConsensusReport` | `stance_matrix` + `rapporteur`（轮值执笔） |
| **Workspace** | `prepare(participant) -> Path` | `ephemeral` / `git_worktree` |

---

## 3. Adapter 层

### 3.1 三个内置适配器覆盖面

- **`openai_compat`** —— 一个适配器吃掉 DeepSeek / Kimi / OpenRouter / Ollama / vLLM / Groq / Together
- **`anthropic`** —— Claude 原生 API
- **`cli`** —— **配置驱动的通用子进程适配器**

### 3.2 `cli` 适配器：加一个 agent = 10 行 YAML

这是本项目"易用性"的核心赌注。接入 codex / gemini-cli / aider / cursor-agent
**不需要改一行 Python**：

```yaml
- id: codex
  adapter: cli
  command: ["codex", "exec", "--json"]
  prompt: stdin              # stdin | argv | argv_template
  cwd: "{workspace}"         # 变量注入
  parse: jsonl               # raw | jsonl | json_path
  extract: "$.message.text"  # parse=json* 时的取值路径
  timeout: 600
  env: {CODEX_QUIET: "1"}
```

流式：`raw` 模式按 stdout chunk 直接吐 Delta；`jsonl` 模式逐行解析。
CLI 适配器通常拿不到 token 数 → 记为 `cost: unknown`，预算按墙钟兜底。

### 3.3 Role（立场注入）

给不同模型分配不同人格是**最廉价的质量增益**——避免同质化附和：

```yaml
role: "务实的系统工程师，优先考虑可维护性与运维成本，对新技术保守"
```

Role 拼进 system prompt，与 Adapter/Model 正交。

---

## 4. Protocol 层

Protocol **只决定"谁在什么阶段发言、他能看到什么"**，其余全部共用，所以每个协议 ≈ 50 行。

```python
class Protocol(Protocol):
    def plan(self, state: DeliberationState) -> list[Phase]: ...

# Move  = (participant, prompt, visible_context, expects_stance)
# Phase = list[Move]   —— 阶段之间串行，阶段内部并行
```

**阶段串行、阶段内并行**——这个形状同时容纳了所有协议（见 4.2）。

### 4.1 四种内置协议

| 协议 | 结构（每轮的 Phase 划分） | 适用 | 成本 |
|---|---|---|---|
| `ensemble` | 1 个 phase：N 人并行出独立初稿，互评立场卡 | 快速取最优解，无需说服 | 最低（单轮） |
| `debate` | 1 个 phase：全员并行质证并修订，回应发生在下一轮 | 有对错的技术选型 | 中 |
| `council` | 1 个 phase：全员并行，且**全互见**（非两两互喂） | 多方权衡的开放问题 | 高 |
| `adversarial` | 3 个 phase：提案 → 全员并行开炮 → 回应/交叉检验（见 4.6） | 找漏洞、安全评审 | 中 |

> 原型的两两互喂在参与者 ≥3 时会退化（A 只看得到 B，看不到 C）。`council` 修掉这一点。
> `adversarial` 中**未被成功反驳的攻击**会原样进入最终报告的"未决项"，无需裁判计分。

### 4.2 为什么是"阶段串行、阶段内并行"，而不是轮流发言

轮流发言（同一轮内一个接一个）有个硬伤：**位置偏见**。

- 第一个发言的人设定了整个讨论的框架（锚定效应）
- 最后一个发言的人信息最多，但也最容易滑向附和
- 墙钟时间是所有人之和，TUI"并排看几个模型同时在写"的画面也没了

并行发言保住了独立性与速度。"同一轮内不能互相回应"这个代价其实不存在——
**回应发生在下一轮**，这正是 debate 协议本来的样子。

真正需要顺序的是**阶段**，不是参与者。以 `adversarial` 为例：

```
Phase 1  提案者出方案            ← 1 人
Phase 2  其余所有人并行开炮       ← N-1 人同时
Phase 3  提案者逐条回应           ← 1 人
```

阶段之间串行（后一阶段依赖前一阶段的产出），阶段内部一律并行。

**两条铁律：**

1. **第 0 轮强制并行**——独立初稿是多样性的唯一来源，
   绝不能让任何参与者先看到别人的初稿
2. 若确实需要串行（`turn_taking: sequential`），**发言顺序每轮 round-robin 轮换**，
   用轮换抵消位置偏见

```yaml
turn_taking: parallel    # parallel(默认) | sequential
```

### 4.3 独任角色一律默认轮值

**凡是某个角色只能由一个人担任，默认一律轮值。**
这是本项目对抗系统性偏向的统一手法——与"不设裁判"同源：
不靠挑一个"公正的人"，而是让位置本身流动起来。

| 独任角色 | 默认 | 防的是什么 |
|---|---|---|
| **执笔人**（`rapporteur`） | `rotate` | 固定执笔带来的措辞偏向 |
| **发言顺序** | round-robin（仅 `turn_taking: sequential`） | 位置偏见 / 锚定效应 |
| **提案者**（`proposer`，仅 `adversarial`） | `rotate` | "测某人的方案" ≠ "选最优方案" |

### 4.4 增量修订，而非全文重写

原型每轮让模型输出完整新版本 → token 指数膨胀，且容易越改越飘。
sesa 默认要求**结构化修订**：`{keep: [...], revise: [{claim, why, new}], drop: [...]}`，
由引擎负责应用并维护当前版本。全文重写降级为 `--full-rewrite` 选项。

---

### 4.5 `adversarial` 的提案者是谁

`adversarial` 是唯一带**不对称角色**的协议，提案从哪来决定了它的两种用法：

```yaml
protocol: adversarial
proposer: rotate     # rotate | <participant_id> | input
```

**`input` —— 被审对象是人给的输入，没有 agent 提案者。**
最常见的用法：`sesa run --file rfc.md "评审这份 RFC"`。
所有参与者都是攻击者，Phase 3 也就不是"提案者回应"，而是**攻击者交叉检验**：

```
Phase 1   （无，提案 = 任务输入）
Phase 2   全员并行开炮
Phase 3   全员并行交叉检验彼此的攻击是否成立
```

交叉检验这一步不能省——否则协议退化成"谁都能随便挑刺"，
最后堆出一坨没验证过的噪音。**只有未被成功反驳的攻击才进入"未决项"。**

**`rotate` —— 每轮换一个人当提案者**，N 轮下来每人都被攻击一遍。
这是唯一无偏的选择；代价是轮次数需 ≥ 参与者数才算公平。

**`<participant_id>` —— 指定某人。**
合法但不对称：这是在红队某个特定方案，而不是在选最优解。运行时会如实标注。

> 默认值：给了 `--file` 则默认 `input`，否则默认 `rotate`。
> 运行时会明确打印实际生效的取值，不让它悄悄生效。

### 4.6 可见性分层：思考过程默认不互见

参与者产出的东西分三层，可见性各不相同：

| 层 | 内容 | 其他参与者可见？ | 落盘？ |
|---|---|---|---|
| **thinking** | 推理草稿（extended thinking） | **否**（默认） | 是，供人查看与追溯 |
| **statement** | 正式发言与论证 | 是 | 是 |
| **stance** | 立场卡 | 是（分歧矩阵的输入） | 是 |

不共享 thinking 的三个理由：

1. **过早趋同是多智能体辩论最大的失败模式。** 共享越多，趋同越快——
   最后花了三份钱，买到一个模型的意见。**独立性是集体智慧的前提**，
   而 thinking 是独立性最脆弱的地方
2. **未定型的想法有污染性。** thinking 里全是自己否决过的路径、试错与自我怀疑。
   B 看到 A"我本来想用 X 但好像不行"，容易被半成品带偏，
   或抓着 A 已经放弃的东西开炮，白白消耗一轮
3. **量级与对等性**（`实测`）。真实议事中 thinking 是正文的 **1.4–2.1 倍**，
   开启共享后 input tokens **+44%**（n=7，两组区间不重叠）；墙钟**无差异**
   （+1%，早前那个 +47% 是 n=1 外推，已更正）；且**不是所有 adapter
   都拿得到**（`claude -p` 不外露思考），强制要求会破坏适配器对等性

> **第 1、2 条都仍未验证。** 曾有一次对照实验（每组 n=7，见 14.3）被读作
> "证伪了第 1 条"，但它判定"无差异"用的三个指标后来全被推翻或降级，
> **那次证伪已撤回**。现在的状态是未知，不是"已排除"。
>
> `never` 保留为默认，理由只剩**省 44% 输入 token**与**适配器对等性**——
> 都与"趋同"无关。

**但留一个开关：**

```yaml
share_thinking: never    # never(默认) | on_deadlock | always
```

`on_deadlock` —— **卡死时才互相开放思考过程**。到了 deadlock，
"过早趋同"已不再是风险；此时最需要的恰恰是挖出双方的前提假设差异（即 §7.2 的"根因"），
而前提假设通常藏在 thinking 里，正文往往只呈现结论。
**在需要独立性的阶段保持独立，在需要挖根因的阶段才开放。**

> **思考 ≠ 工作痕迹。** CLI agent"读了 `src/db.py`，发现已经在用 JSONB"
> 属于**证据**而非思考，事实性发现应当共享——文本任务靠 prompt 要求引用出处，
> 代码任务走 Evidence 通道（见第 6 节）。

---

## 5. Consensus 层（项目的心脏）

### 5.1 立场卡

每轮每个参与者除正文外，额外输出一份结构化立场卡：

```json
{
  "position": "一句话概括我的立场",
  "confidence": 0.75,
  "key_claims": ["...", "..."],
  "stance_on": {
    "kimi":  {"verdict": "agree",    "reason": ""},
    "gpt5":  {"verdict": "disagree", "reason": "他默认了单机部署，与题设矛盾"}
  },
  "open_questions": ["..."],
  "changed_from_last_round": true
}
```

**容错抽取**：找最后一个 ```json fence → 找最后一个配对的 `{...}` → 仍失败则
**要求该参与者重发一次纯立场卡**；再失败则该轮该参与者立场记为 `unknown`，
在报告中显式列出——**不猜、不代笔**。

某参与者 `unknown` 轮数过多时，`doctor` 会建议为它关闭立场卡、改用简化协议。

### 5.2 收敛判定

**default-deny**：一格算"已解决"，当且仅当存在**可解析的显式 `agree`**。
`disagree`、`unknown`、以及**空载荷的 `partial`** 一律计入 `unresolved`——
引擎手里没有某人的判断时，不能替他默认同意。

```
unresolved   = #{ (i,j) : verdict ∈ {disagree, unknown} }   硬分歧，阻塞收敛
reservations = #{ (i,j) : verdict == partial（带残差） }      软保留，只降档
```

- **consensus**：`unresolved == 0`、`reservations == 0`、`min(confidence) >= 阈值`
- **consensus_with_reservations**：无硬分歧，但有登记在案的残差
- **deadlock**：连续 K 轮（默认 2）无人改变立场且 `unresolved` 未下降
- **exhausted**：超过 `max_rounds` 或触发预算熔断

> **共识是报告标签，不是终止条件。** 终止由轮数与预算硬保证，
> 因此把判定收紧到 default-deny 不会导致"辩论永远跑不完"，
> 最坏情况只是报告如实说没谈拢。这条区分是两次真实议事的核心产出——
> 我们此前把二者混为一谈，才误以为"严格判定会让工具退化"。

**`partial` 必须携带非空残差。** 空载荷的"部分同意"无法验证：它既不说明
同意什么，也不说明保留什么，因此按未表态处理。

> **只能对看到过的人表态。** 参与者被要求填写 `stance_on` 时，
> 名单里只包含他**确实读过发言**的人。第 0 轮谁也没看过谁，此时若还要求
> 表态，模型只能凭空捏造——实测中 DeepSeek 的发言里"kimi"出现 0 次，
> 立场卡却对 kimi 打了分。若各方碰巧都猜 `agree`，引擎就会在
> **一轮未经交锋的辩论后宣布共识**，整个议事的意义被架空。
>
> 配合"未表态即阻塞共识"这条规则，**第 0 轮在结构上不可能收敛**。

### 5.3 三种终局，如实标注

| 状态 | 含义 | 报告呈现 |
|---|---|---|
| `consensus` | 真的谈拢了 | 裁决书 + 一致结论 |
| `consensus_with_reservations` | 无人反对，但有残差 | 结论 + **保留意见清单原样进交付物** |
| `deadlock` | 卡死 ≠ 统一 | **明写"未达成共识"** + 各方立场并列 + 分歧矩阵，交给人拍板 |
| `exhausted` | 预算/轮数烧完 | 明写"讨论未完成" + 当前最优 + 未决项 |

> 这是本项目的诚实性底线：**不把僵局粉饰成共识。**

### 5.4 为什么不设仲裁者

原型的毛病是"裁判即选手"。常见解法是引入第三个中立模型当裁判——但那要求用户多配一个 key，
而且**裁判自己也可能错**，等于用一个未经辩论的观点覆盖辩论结果。

sesa 的选择是**取消裁判**：

- **达成共识时**，共识本身就是产出——不需要第三方再合成一遍
- **未达成共识时**，如实并列各方立场 + 分歧矩阵，**由使用者拍板**

这既消除了偏向，也消除了首次使用的最大摩擦（不需要额外 API key）。

### 5.5 执笔人（Rapporteur）

立场卡都写 `agree` 不等于正文真的一致——措辞、细节可能仍有出入。
所以收敛后需要有人把多份文本整合成一份，这个角色叫**执笔人**。

- 从**参与者中轮值**产生（默认按轮次轮换，`--rapporteur <id>` 可指定）
- **不是裁判**：职责是整合措辞、归因分歧，而非裁定对错
- **无论是否收敛都动笔**——哪怕吵翻了，谈拢的部分也要被正式写成结论，
  只把真正没谈拢的留进"未决分歧"（详见第 7 节）。
  把两份互相矛盾的长文原样丢给人，等于把活推回去了
- 产出附 `drafted_by` 字段，报告中明示由谁执笔

**假共识检测（双向）**：

1. 执笔人在 `conflicts_found` 里显式报告了冲突
2. **矩阵称一致，执笔人却列出了未决分歧** —— 这个方向更要紧：
   那是执笔人读完全文后，抓到了结构化立场卡没能反映出来的实质冲突

任一命中即判定 `false_consensus`，并在事件流中如实记录。

---

## 6. Workspace 与 Evidence（文本任务 ↔ 代码任务）

文本任务与代码任务只在两个接缝上不同，其余四层共用。

| | `ephemeral`（文本议题） | `git_worktree`（代码任务） |
|---|---|---|
| 隔离 | 临时目录，不碰 repo | `git worktree add` 每人一份，互不踩踏 |
| 参与者 cwd | 临时目录 | 各自的 worktree |
| Evidence | 无 | 跑 `--verify` 命令，退出码/输出作为客观证据入场 |
| 裁决产物 | markdown 结论 | 选定/合并的分支 + 裁决理由 |

### 6.1 Evidence 钩子

```bash
sesa run --repo . --verify "pytest -q" "修复 issue #123"
```

每轮各 worktree 跑一次 verify，结果注入下一轮上下文：

```
[EVIDENCE] claude/worktree  → pytest: 12 passed
[EVIDENCE] kimi/worktree    → pytest: 10 passed, 2 failed (test_tz_boundary)
```

执行结果比修辞硬得多。但——

### 6.2 证据也会错

**"执行结果是客观裁判"是一句写得太满的话。** 证据至少有六种出错方式，
其中第一种在 agent 场景里几乎是默认行为：

| 出错方式 | 例子 |
|---|---|
| **写代码的人也写测试** | 绿灯什么都不证明：可能是 `assert True`，也可能断言恰好匹配了自己的 bug |
| **伪造引用** | "我读了 `src/db.py`，发现已在用 JSONB"——其实没读，或读错了 |
| **测试跑偏** | 通过了，但压根没测到争议的那一点 |
| **环境差异** | 两个 worktree 依赖/缓存不同，同一条命令结果不同，谁都不算错 |
| **证据过期** | 第 1 轮的结果被拿到第 3 轮引用，代码早改了 |
| **动手脚** | 直接改测试文件让它变绿 |

因此 Sesa 对证据的处理遵循四条规则。

#### 规则一：引擎绝不采信参与者转述的执行结果

证据按来源分级，这是结构性的第一道闸门：

| 来源 | 含义 | 地位 |
|---|---|---|
| `engine` | **引擎自己**在受控工作区里执行得到的 | 才叫证据 |
| `claimed` | 参与者自述"我跑了，结果是……" | 只是**待核验的主张**，与其他论断同级 |

参与者说的一切关于命令输出的话，默认是待核验主张，不是事实。

#### 规则二：引用可机械核验

文本任务里参与者引用文件与行号时，引擎直接去查：文件在不在？
引的那段话真的在里面吗？这个检查很便宜，却能抓住绝大多数幻觉引用。
核验不通过的引用在报告中标注，并且不得作为共识依据。

#### 规则三：交叉测试——防"自己测自己"的真正解法

既然自测不算数，就**拿 A 的测试去跑 B 的实现**：

```
                claude 的实现   kimi 的实现
claude 的测试        ✅              ❌
kimi 的测试          ✅              ✅
```

这张表信息量极大：kimi 的实现挂在 claude 的测试上，而 claude 的实现能通过双方的测试。
若某人的测试**只有他自己能过**，要么是测试编码了他的私有假设，要么是别人真错了——
无论哪种，**分歧都被精确定位到了具体一条测试上**，比吵十轮都有用。

> 立场矩阵是嘴上的，**交叉测试矩阵是手上的**。两者形成对称。

此外，用户提供的基线测试**任何人不得修改**：引擎用 `git diff` 检查受保护路径，
被动过就是一次严重违规，如实记入报告。

#### 规则四：证据可被反驳

证据进入辩论时是"**强的、但可反驳的主张**"，而不是审判。
参与者可以主张"这条测试本身是错的，因为……"，而该反驳同样可被核验。

因此**不是"与执行结果矛盾的立场自动作废"，而是必须被显式回应**——
未回应的矛盾直接进入最终报告的"未决项"。

#### 证据的元数据

每条证据都带上来源、被检验方与工作区版本指纹，
代码一改，旧证据即失效，不允许跨轮复用：

```python
EvidenceRecord(
    participant="claude",   # 提出者 / 测试作者
    against="kimi",         # 被检验方；等于 participant 即"自己测自己"
    source="engine",        # engine | claimed
    revision="a1b2c3d",     # 工作区版本，代码变更后本条失效
    cmd="pytest -q", exit_code=1, summary="...",
)
```

---

## 7. 产出形态

### 7.1 一致与不一致不是两种产出

**执笔人永远动笔**，只是写的范围不同。哪怕完全没谈拢，那些已经谈拢的部分
也应该被正式写成结论，只把真正没谈拢的一小块留出来。

`RESULT.md` 的骨架**恒定不变**：

```markdown
# <任务>

## 结论            ← 一致的部分，直接可用（标注 drafted_by）
## 共识依据        ← 支撑结论的关键论点，标注谁在哪一轮认同 / 改变了立场
## 未决分歧        ← 见 7.2
## 少数意见        ← 被多数否决但持有者仍坚持的，原样保留
```

| 终局 | 文档形态 |
|---|---|
| 全一致 | "未决分歧"为空，这就是一份干净的答案 |
| 部分一致 | **最常见也最有价值**：结论写共识部分，未决写剩下的 |
| 完全不一致 | 结论写"无共识"，未决成为文档主体 |

使用者的阅读方式永远一致，不会因为吵没吵拢就换一种文档格式。

### 7.2 未决分歧的写法

简单并列各方立场是不够的——那只是把归纳的活推回给人。
每一条未决分歧必须包含三样东西：

```markdown
### 分歧 1：部署规模的前提假设
| 参与者 | 立场 | 理由 |
|---|---|---|
| claude | 单机 Postgres 足够 | 现有量级下运维成本更低 |
| gpt5   | 必须上分片 | 按三年增长曲线会撞墙 |

**根因**：双方对峰值 QPS 的假设不同（claude 假定 ~500，gpt5 假定 ~50000）
**决断所需信息**：你们实际的峰值 QPS 量级？
**下一步**：sesa resume <run_id> --inject "峰值 QPS 约 3000"
```

1. **根因归属** —— 分歧绝大多数源于**前提假设不同**而非结论对错。
   把假设摊开，人往往一眼就能拍板
2. **决断所需的那一条信息** —— "你只要告诉我 X，这个分歧自动消解"
3. **可执行的下一步** —— `resume` 从断点继续辩，无需从头重跑

> `resume` 让 deadlock 从"失败"变成循环中的正常中间态：
> 吵不拢 → 告诉你缺什么 → 你补上 → 接着吵。

### 7.3 代码任务的对应产出

| 文本任务 | 代码任务 |
|---|---|
| 结论（markdown） | 合并分支 `sesa/<run_id>/result` |
| 共识依据 | 各方都通过的测试清单 |
| 未决分歧 | 有争议的文件与实现选择 + **各方分支全部保留可 checkout** |
| 少数意见 | 未被采纳但保留的实现（分支不删） |

```
sesa/<run_id>/claude    ← 保留，可 git diff
sesa/<run_id>/kimi      ← 保留
sesa/<run_id>/result    ← 执笔人合并结果
```

**不删任何参与者的分支。** 人可以自己 diff 两份实现再定夺。

### 7.4 落盘结构

```
.sesa/runs/<run_id>/
├── RESULT.md      # 主产出，直接可用
├── RESULT.json    # 同一份的结构化版本，供 MCP / 第三方产品消费
├── REPORT.md      # 议事纪要：怎么吵出来的、分歧矩阵演变、成本明细
├── events.jsonl   # 原始事件流，可回放、可评测
└── turns/         # 每人每轮原文，供追溯
```

`RESULT.md` 是 `RESULT.json` 的渲染，两者永远同源。

---

## 8. 事件流（对外契约）

Engine 唯一的输出。CLI / TUI / SDK / MCP / 第三方产品消费的是同一份。

```jsonc
{"t":"run.start",       "run_id":"...", "task":"...", "participants":[...], "protocol":"debate"}
{"t":"round.start",     "round":2}
{"t":"turn.start",      "round":2, "participant":"kimi"}
{"t":"turn.thinking",   "participant":"kimi", "text":"..."}      // 只落盘，不进他人上下文
{"t":"turn.delta",      "participant":"kimi", "text":"..."}          // 流式
{"t":"turn.end",        "participant":"kimi", "tokens":{...}, "usd":0.012}
{"t":"stance.emit",     "participant":"kimi", "stance":{...}, "degraded":false}
{"t":"evidence",        "participant":"kimi", "cmd":"pytest -q", "exit":1, "summary":"..."}
{"t":"consensus.update","round":2, "unresolved":1, "matrix":[...], "state":"open"}
{"t":"false_consensus", "round":3, "detected_by":"kimi", "conflicts":[...]}   // 退回再辩
{"t":"human.inject",    "text":"忽略成本因素，只看可维护性"}          // 人在环中
{"t":"run.resume",      "from_run":"...", "inject":"峰值 QPS 约 3000"}       // 断点续辩
{"t":"budget.warn",     "spent_usd":1.6, "limit_usd":2.0}
{"t":"verdict.final",   "outcome":"consensus", "answer":"...", "drafted_by":"kimi",
                        "dissent":[...], "unresolved":[...]}
```

全量落盘为 `.sesa/runs/<run_id>/events.jsonl`，支持：
- `sesa replay <run_id>` —— TUI 回放
- `sesa report <run_id>` —— 渲染 markdown
- 后续做评测：哪种"agent × 模型 × 协议"组合在什么任务上更好

---

## 9. 人在环中（TUI 的真正价值）

多 agent 辩论天生是**可观战、且该被打断**的。原型用 tmux 分屏是在手工模拟这件事。

Textual TUI 提供：

```
┌─ sesa ─ debate ─ round 2/4 ─ $0.41 ────────────────────┐
│ ┌ claude ────────┐ ┌ kimi ──────────┐ ┌ gpt5 ─────────────┐ │
│ │ 我仍认为应该… ▊ │ │ 同意 claude 的… │ │ 两位都忽略了…   ▊ │ │
│ └────────────────┘ └────────────────┘ └───────────────────┘ │
│ ┌ 分歧矩阵 ───────────────────────────────────────────────┐ │
│ │          claude   kimi    gpt5                          │ │
│ │ claude      -     agree   ✗ 部署规模前提                │ │
│ │ kimi      agree     -     ✗ 部署规模前提                │ │
│ │ gpt5        ✗       ✗       -                           │ │
│ │ 未决分歧 1 项 · 状态 open                                │ │
│ └─────────────────────────────────────────────────────────┘ │
│ [i] 插话  [v] 否决前提  [f] 跟随某方  [s] 提前收束  [q] 退出 │
└──────────────────────────────────────────────────────────────┘
```

干预动作（均落为事件，可回放）：
- **插话** —— 追加约束进入下一轮上下文
- **否决前提** —— 宣告某条 claim 无效，参与者必须绕开它
- **跟随某方** —— "就按 gpt5 的思路继续"
- **提前收束** —— 立即让执笔人按当前状态成文

> `textual serve` 可把同一个 TUI 一键变成 Web 界面，无需二次开发。

---

## 10. 预算与安全

**thinking 模型的隐性成本**（`实测`）：推理 token 计入 output。
实测 Kimi 的输出中 **76%** 是推理内容（正文仅占 24%），个别配置下高达 87%。
而议事本身**并不消费**推理——它默认不进其他参与者的上下文。
成本敏感场景应显式关闭：`extra_body: { thinking: { type: disabled } }`。

不同计费方式的实际差异也很大，选参与者时值得一并考虑：

| 通路 | 计费 | 实测 output/input |
|---|---|---|
| agent CLI（订阅制，如 `claude -p`） | 不按 token | 拿不到用量 |
| DeepSeek API | 按 token | 0.76 |
| Kimi API（thinking 开启） | 按 token | **2.2** |

**预算熔断**：`max_usd` / `max_tokens` / `max_wall_seconds`，任一触顶即停并出临时裁决。
API 适配器读真实 usage；CLI 适配器多半拿不到 → 记 `unknown`，由墙钟兜底。

**安全**（代码任务默认涉及 `--dangerously-skip-permissions` 一类开关）：
- 默认 `ephemeral` 工作区，**不碰用户 repo**
- 代码任务必须显式 `--repo`，且必须在干净的 git 仓库中（有未提交改动则拒绝）
- worktree 隔离，参与者写不到彼此和主分支
- `--yolo` 之类的危险开关必须由用户在 config 中逐参与者显式开启，不提供全局默认开

---

## 11. 配置与凭据

参与者配置**填一次，长期复用**——这是首次使用体验的关键。

### 11.1 两级配置

```
~/.config/sesa/config.yaml   # 全局参与者库，跨项目复用
./sesa.yaml                  # 项目级：本项目用哪几个、什么协议、什么预算（覆盖全局）
```

**参与者数量由用户决定：最少 2 个，无上限**（只受预算约束）。
2 人时分歧矩阵只有一格——这不是缺陷，因为本来就由人拍板；人多时多数/少数意见才有意义。

### 11.2 `sesa init` 向导

1. **探测本机已装的 agent CLI**（claude / codex / dsh / gemini / aider / cursor-agent），勾选
2. **添加 API 模型**：选预设（DeepSeek / Kimi / OpenRouter / Ollama / 自定义 base_url）
3. **为每个参与者写 role**（可跳过，用默认人格）
4. **凭据处理**（见 11.3）
5. 写入全局配置，之后任何项目直接 `sesa run` 即可

### 11.3 凭据

**默认不把 key 明文写进配置文件。**

| 方式 | 配置里记什么 | 说明 |
|---|---|---|
| 系统钥匙串（默认） | `api_key: keyring` | macOS Keychain / Windows Credential Manager / Secret Service |
| 环境变量 | `api_key_env: KIMI_API_KEY` | 适合 CI |
| 明文（不推荐） | `api_key: sk-...` | 仅在向导明确警告后允许，文件权限强制 600 |

### 11.4 参与者管理

```bash
sesa participants list
sesa participants add           # 走向导
sesa participants test kimi     # 单发一条，验证可用性与延迟
sesa participants remove kimi
```

### 11.5 配置样例

```yaml
version: 1

participants:
  - id: claude
    adapter: cli
    command: ["claude", "-p", "--effort", "high"]
    prompt: stdin
    cwd: "{workspace}"
    role: "务实的系统工程师，优先可维护性与运维成本"

  - id: kimi
    adapter: openai_compat
    base_url: https://api.kimi.com/coding/v1
    model: kimi-for-coding
    api_key: keyring                  # 实际的 key 存在系统钥匙串
    role: "激进的创新者，敢于挑战既有假设"

  - id: gpt5
    adapter: openai_compat
    base_url: https://openrouter.ai/api/v1
    model: openai/gpt-5
    api_key_env: OPENROUTER_API_KEY
    role: "严谨的怀疑论者，专找论证漏洞"

# 不设仲裁者。收敛后由参与者轮值执笔；未收敛则并列呈现，由人拍板。
# 独任角色一律默认轮值（见 4.3）。
rapporteur: rotate                    # rotate | <participant_id>
proposer: rotate                      # 仅 adversarial：rotate | <id> | input
turn_taking: parallel                 # parallel(默认) | sequential
share_thinking: never                 # never(默认) | on_deadlock | always

protocol: debate
rounds:    {max: 4, stability_window: 2}
consensus: {confidence_threshold: 0.6}
budget:    {max_usd: 2.0, max_wall_seconds: 900}
```

---

## 12. CLI

```bash
sesa init                    # 首次向导：探测已装 agent CLI + 配 API 模型 + 存凭据
sesa doctor                  # 逐个检查可调用性与认证
sesa participants list|add|test|remove

# 文本议题
sesa run "该用 Postgres 还是 SQLite？" -p claude -p kimi -p gpt5
sesa run --file rfc.md "评审这份 RFC" --protocol adversarial

# 代码任务
sesa run --repo . --verify "pytest -q" "修复 issue #123"

sesa resume <run_id> --inject "峰值 QPS 约 3000"   # 补充信息后从断点续辩
sesa replay <run_id>         # TUI 回放
sesa report <run_id>         # 渲染 markdown
```

默认：**是 tty 就开 TUI；被管道接走就吐 JSONL**。`--tui` / `--json` 可强制。

---

## 13. 路线图

| 版本 | 内容 |
|---|---|
| **v0.1** | 流式内核 + 事件流 + 三个 Adapter + `init` 向导与配置持久化 + `ensemble`/`debate` + 立场矩阵共识 + 轮值执笔人 + `RESULT.md`/`RESULT.json` 产出 + `ephemeral` 工作区 + CLI + JSONL 落盘 |
| **v0.2** | ~~Textual TUI（观战 + 四种人工干预）~~ 已落地 + `replay` + `resume --inject` 断点续辩 |
| **v0.3** | `git_worktree` 工作区 + Evidence 钩子（引擎执行 + 交叉测试矩阵 + 引用核验 + 基线测试防篡改）+ `council`/`adversarial` |
| **v0.4** | MCP server（让 Claude Code 等 agent 自己"开个议会"）|
| **v0.5** | 评测扩展：基准题库 + 跨配置对照实验（`sesa eval` 的指标层已在 v0.1 落地） |

---

## 14. 实测结论

设计里的论断分三档标注：**`实测`**（跑过实验或有生产数据）、
**`推断`**（有机制解释但未验证）、**`未验证`**（仅凭合理性成立）。
这是把项目自身的价值观用在自己身上——我们要求参与者区分事实与判断，
文档不该例外。

### 14.1 已被实测推翻的论断

**`CLI agent 对结构化输出的服从性明显更差`** —— **假**。

| 组件 | JSON 立场卡 | 逐行表 | 耗时对比 |
|---|---|---|---|
| claude CLI | 3/3 | 3/3 | 41–61s vs **7–8s** |
| kimi CLI | 3/3 | 3/3 | 51–118s vs **15–16s** |

生产数据一致：三场真实议事的立场卡降级率为 0.00 / 0.17 / 0.00，
而那个 0.17 经查是**我们自己抽取器的缺陷**（参与者引用了别的 JSON 时
提前返回，永不走括号配对兜底），不是参与者不守格式。

> 这条前提曾被用来论证 T2 降级阶梯、`cli` 适配器的产物文件通道等多处设计。
> 那些设计仍然值得保留，但**理由要换**：不是"JSON 会失败"，
> 而是"降级路径便宜、快 4–7 倍，且在真失败时只丢一格"。

### 14.2 已撤回的论断：`多轮辩论确实改变结论`

这曾是本项目的存在性前提，并一度被标为"已实测支持"（立场漂移 0.62–0.92）。
**该结论已撤回**——不是被证伪，而是**支撑它的指标经校准后失效**。

**指标校准**（对已知情形手工构造，零成本）：

| 情形 | 词面漂移取值 |
|---|---|
| 完全相同 | 0.000 |
| **结论完全相反** | **0.394** |
| **同结论、不同措辞** | **0.733** |
| 同结论 + 长附加条件 | 0.881 |
| 话题完全无关 | 1.000 |

**在关键区间里它是反的**："换个说法"得分高于"结论相反"。
而真实议事的取值（0.62–0.92）恰好落在"同结论不同措辞"那一档——
这个指标很可能一直在测**措辞变化**，而不是立场变化。

**改用不依赖词面的类别指标后**（agree/partial/disagree 的跨轮变化，
并排除"第 0 轮 unknown → 第 1 轮首次表态"这类并非改变主意的转换）：

```
33 场议事中，第一次表态之后真正改变判断的格子总数：  3
参与者自述「我改变了立场」的总次数：               33
```

**但类别指标同样不足以定论**：实测中绝大多数格子长期停在 `partial`，
类别空间实际只有一个值，它测不出 `partial` 内部的移动——
而移动很可能就发生在**残差**（"我还没接受哪些点"）的增减里。

> **当前诚实的状态：我们没有一个有效手段测量"辩论是否改变立场"。**
> 词面指标测的是措辞，类别指标分辨率不够。存在性论断的状态是
> **"未能有效测量"**，既非成立也非证伪。
>
> 已做的补救：残差此前**根本没进事件流**（`stance.emit` 只带 verdict），
> 现已补上，并新增 `residual_churn` 指标。待新数据积累后重新评估。

**这条教训比结论本身重要**：我们用一个未经校准的指标跑了二十多场实验、
并据此写进 README，直到自己觉得"这个数怎么几乎不随干预变化"才去校准。
**先校准指标，再用它下结论。**

**`双向假共识检测在做实事`** —— **真**，三场全部命中真实冲突。
但判据一度过宽（对 `consensus_with_reservations` 也生效），
导致 3/3 误报——该档的定义本就是"有保留意见"，
执笔人把保留写成未决分歧是预期行为。现已收窄到只对完全共识生效。

### 14.3 一条已撤回的证伪：共享思考与趋同

**`共享思考会导致过早趋同`** —— **假**（对照实验，每组 7 次有效运行）。

同一议题、同样两位参与者（Kimi + DeepSeek）、同样三轮，只改 `share_thinking`：

| 指标 | `never` | `always` | 判断 |
|---|---|---|---|
| 立场漂移 | 0.623 ±0.090 | 0.661 ±0.048 | +6%，在标准差内，无差异 |
| 参与者间发散度 | 0.685 ±0.076 | 0.733 ±0.046 | +7%，在标准差内，无差异 |
| 承认改变立场 | 0.86 ±0.69 | 0.86 ±0.69 | **完全相同** |
| 轮数 | 3.00 | 3.00 | 都跑满 |
| 墙钟 | 282s ±40 | 283s ±66 | +1%，无差异 |
| **input tokens** | **8477 ±305** | **12205 ±1248** | **+44%，两组区间完全不重叠** |

若趋同效应存在，`always` 组的漂移与发散度应当显著更低——没有。
更直接的是"承认改变立场"这一项两组**完全相同**。

> **样本量增长如何改变结论**，这本身值得记下来：
>
> | 指标 | n=1 | n=4 | n=7 |
> |---|---|---|---|
> | 改变立场 | +100% | +100% | **0%** |
> | 墙钟 | +47% | −3% | **+1%** |
> | input tokens | +54% | +42% | **+44%** |
>
> 前两项在 n=1 时看起来是强信号，样本一多就归零；
> 只有有机制解释的 token 增幅从头稳到尾。

> **⚠️ 本节的证伪已撤回（2026-08-30）。**
>
> 上表里判定「无差异」的三个指标，后来都被推翻或降级：
>
> | 指标 | 后来的状态 |
> |---|---|
> | 立场漂移 | ❌ 校准后失效——它测的是**措辞**，「同结论不同措辞」得分高于「结论相反」 |
> | 参与者间发散度 | ❌ 已废弃，同源问题 |
> | 承认改变立场 | ⚠️ 自述项，实测高报约 11 倍 |
>
> 三根柱子拆掉两根半，**「共享思考不会加速趋同」这句话不再有依据**。
> 现在的状态不是「已证伪」，是**未知**。
>
> 仍然成立的只有两项客观量：轮数与墙钟无差异、input tokens +44%
> （两组区间不重叠，有机制解释）。它们不回答趋同问题。
>
> 更要紧的是**这条撤回本身**：一个结论的支柱被推翻后，结论不会自动
> 跟着倒——除非有人回头去看。本节在两根柱子被拆掉之后，又以「已证伪」
> 的姿态挂了很久。**证据台账必须能反向索引：某个指标失效时，
> 要能立刻问出「哪些结论是靠它站着的」。**
>
> 这条反向索引现在是一条测试（`tests/test_evidence_ledger.py`）：
> 文档里凡是提到已失效指标的段落，**必须同一段带上失效标注**，
> 否则 CI 红。第一次跑它就抓出三处漏网——14.4 的主指标、
> §4.6 的一段陈述、以及文献对照里"把漂移当正面信号"那句。
> **原则写下来不会自动生效，得让它可执行。**

**因此 `never` 仍作默认，但理由只剩两条，且都与"趋同"无关：**

1. **省 44% 输入 token**（实测，两组区间不重叠，有机制解释）
2. **适配器对等性**：`claude -p` 不外露思考，强制共享会让不同适配器不可比

> 实验边界：2 名参与者、单一议题、两个特定模型、每组 7 次。
> 这不足以支持"共享思考在任何场景都无害"，只足以否证
> "在本设置下共享会明显加速趋同"。

**两处需要更正的早前论断**（都源自 n=1 外推）：

| 早前说法 | 依据 | n=4 实测 |
|---|---|---|
| 思考体量是正文的 41 倍 | 一道小数比较题 | 真实议事负载下 **1.4–2.1 倍** |
| 共享思考墙钟 +47% | 单次运行 | **+1%**（n=7），纯噪声 |

> 同一轮里两次"从单样本外推"，正是这个项目一直在防的错误。
> **n=1 只能提出假设，不能支持论断。**

### 14.4 一条已降级的论断：role 与同质化

**`立场倾向（role）能避免同质化附和`** —— **主预测未成立**
（异模型对照，每组 n=8：Kimi + DeepSeek，一组配对立的 role，一组都不配）。

| 指标 | with-role | no-role | 判断 |
|---|---|---|---|
| **参与者间发散度**（主指标） | 0.857 ±0.049 | 0.839 ±0.073 | +2%，区间大幅重叠 → **无效** |
| 首轮发散度 | 0.850 ±0.091 | 0.813 ±0.097 | +5%（n=4 时为 +10%，随样本衰减）→ 噪声 |
| 立场漂移 | 0.774 ±0.069 | 0.779 ±0.085 | −1% |
| input tokens | 9028 ±358 | 9102 ±438 | −1%，成本中性 |
| **改变立场次数** | **0.62 ±0.52** | **1.38 ±0.74** | **−55%** |
| **假共识出现** | **3 / 8** | **0 / 8** | |

> **⚠️ 主指标已失效（2026-08-30 补记）。**
>
> 上表的主指标"参与者间发散度"与"立场漂移"后来都被推翻——它们测的是
> **措辞**而非立场（校准显示"同结论不同措辞"得分高于"结论相反"）。
> 因此 **"role 没有提高发散度"这句话不再有依据**，本节的主预测结论
> 应读作**未测量**，而非"已证否"。
>
> 下半节那个反向信号（"改变立场次数 −55%"）**同样要打折**：它是自述项，
> 实测高报约 11 倍。分布分离（with-role 最多改 1 次、no-role 有四次改 2 次）
> 仍是个值得追的线索，但不构成结论。
>
> 仍然成立的只有两项与措辞无关的客观量：**input tokens 成本中性**、
> **假共识 3/8 vs 0/8**。后者是引擎自己的双向校验判出来的，不依赖任何
> 语义指标——**这是本节唯一站得住的发现，而它恰恰不是当初要测的那条**。

Role 没有提高参与者间的发散度——这是它被引入时声称的唯一收益。

**但最后两行是个方向相反的信号。** 与首轮发散度那条随样本衰减的线索不同，
"改变立场次数"从 n=4 的 −50% 到 n=8 的 −55% **反而更稳**，且分布有实质分离：
`with-role` 八次里最多改 1 次，`no-role` 有四次改了 2 次。

机制上讲得通：给参与者一句"你认为机制越少越好"，**本身就是一条"守住立场"的
指令**。对一个价值主张是"让辩论改变结论"的引擎来说，这个方向不对。

> **暂不据此改动默认值。** 本实验测的是"模型已经不同时 role 还有没有增量"，
> 而 role 真正要救的场景是**同一个模型扮演不同角色**——那时模型差异不再提供
> 多样性，role 是唯一的差异化手段。该对照实验待补。

### 14.5 一个反复出现的方法论错误

到目前为止我造了三个指标，**三个都不测量它们名字所指的东西**；
后来又有第四次，形态略有不同但同源（14.17）：

| 指标 | 我以为它测什么 | 它实际测什么 |
|---|---|---|
| `position_drift` | 立场变化 | **措辞变化**（"同结论不同措辞"得分高于"结论相反"） |
| `residual_churn` | 保留意见的推进 | **1 bit**（集合相不相等，同义改写即满分） |
| `laundering_index` | 换措辞重列 | **换手率**，分不出"换措辞"与"推进到新问题" |
| （弱模型代码分）| 模型的实现判断 | **在没有规格的情况下猜的结果**——见 14.17 |
| （自审四轮的辩论动力学）| 辩论怎么起作用 | **一个我造出来的配置里**发生了什么——见 14.22 |

共同的错误形状：**造一个廉价的计数代理，用想测的概念给它命名，
然后把数字当成那个概念已经被验证了。** 名字一旦贴上，
后续所有解读都默认它成立——而校准往往是很久以后才想起来做的事。

最后一个的翻转过程值得记：换手率在六场议事里都是 0.89–1.0，
我据此在 `sesa eval` 里加了"保留意见在被换措辞重列"的预警。
**逐条读原文后发现恰恰相反**——第 2 轮的保留意见接住了对方第 1 轮的
回应，推进到了新的点上。指标数值一样，含义相反。

> **计数方法无法区分"洗"与"深入"。** 要区分必须做语义比对
> （嵌入或判官），这是绕不过去的成本。在付出这个成本之前，
> 只能说一件可靠的事：**未决保留意见的总数不随轮次下降。**

### 14.6 语义比对：那个悬了很久的问题终于有答案

计数方法答不了"辩论是在洗残差还是在推进"，因此引入本地嵌入做语义比对。
**这次先校准再用**——三个候选模型跑同一组已知情形：

| 模型 | 改写最低 | 新问题最高 | 间隔 |
|---|---|---|---|
| paraphrase-multilingual-MiniLM-L12-v2 | 0.620 | 0.700 | **−0.081** ❌ |
| BAAI/bge-small-zh-v1.5 | 0.756 | 0.849 | **−0.093** ❌ |
| **BAAI/bge-base-zh-v1.5** | 0.717 | 0.602 | **+0.115** ✅ |

前两个在关键区间是**反的**：给"同话题不同主张"的分比"同义改写"还高——
它们抓的是话题而非主张。小模型尤其明显（bge-small 给了 0.849）。
**若按老习惯装上就用，又会得到一个看起来合理、实则反向的指标。**

校准通过后应用到真实数据：

| 场次 | 换手率 | 语义改写率 | 含义 |
|---|---|---|---|
| claude ×2 第一场 | 0.89 | **1.00** | 全是改头换面 |
| claude ×2 第二场 | 1.00 | **0.40** | 参半 |
| DeepSeek ×2（回灌 on）×2 | 0.92 / 1.00 | **0.00** | 全是新问题 |
| DeepSeek ×2（回灌 off）×2 | 0.91 / 0.91 | **0.00** | 全是新问题 |

**换手率全在 0.89–1.00 几乎一样，语义改写率却跨了 0.00–1.00 整个区间。**
换手率对真实差异毫无分辨力。

> **顺带更正一处我自己的过度推广。** 前文根据手工阅读残差原文，
> 断言"这是推进到新问题，不是洗残差"。语义比对显示：
> 我读的恰好是 DeepSeek 那场（改写率 0.00，结论对），
> 但 claude 那两场是 1.00 与 0.40。**结论对，推广错了。**
> 单场手工阅读不足以支持跨场结论——这与 n=1 那条教训同形。

### 14.7 对这套评估本身的审视

指标通过校准不等于结论成立。对语义比对做了一次效度审视，
**结果推翻了 14.6 的强版本结论**：

**威胁一：阈值落在真实数据正中间。** 51 条新增残差的相似度分布为
0.441–0.813，中位 0.634；阈值 0.66 落在**第 65 百分位**——
分类边界最不稳的位置。

**威胁二：结论对阈值极度敏感。**

| 阈值 | 整体改写率 | claude 场 | DeepSeek 场 |
|---|---|---|---|
| 0.50 | 0.94 | 1.00 | 0.88 |
| 0.60 | 0.57 | 0.93 | 0.17 |
| 0.66 | 0.35 | 0.67 | 0.00 |
| 0.80 | 0.02 | 0.04 | 0.00 |

**14.6 报告的"claude 全是改头换面(1.00)、DeepSeek 全是新问题(0.00)"，
是阈值切在两组分布之间造成的假象**，不是现象本身的二分。阈值挪 0.05 结论就翻。

**威胁三：校准集小且由我自撰。** 原 5 条里"逐字相同"是送分题，
真正定下"改写最低分"的只有 1 条。三个模型用同一个 5 条集合选优，
接近于在噪声上挑赢家。已补入两条取自真实残差原文的样本
（0.821 / 0.395，判定正确，间隔未变）。

**威胁四：残差是参与者自报的。** `residuals_discharged` 把"没再列"
当作"不再坚持"，但也可能只是懒得重复——与"自述改变立场过报 11 倍"
是同一个不可靠源。参与者自己提过修法（显式 `withdrawn` 标记），未实现。

**威胁五：六场议事、两种模型配置、一个议题体裁**（永远是度量/机制设计）。

#### 审视后仍然站得住的结论

| 结论 | 状态 |
|---|---|
| claude 场的新残差与上一轮更相似（0.741 / 0.652）<br>DeepSeek 场更不相似（0.606 / 0.585 / 0.541 / 0.524） | ✅ **稳**，在 0.50–0.80 全部阈值下排序不变 |
| "一组全是改写、另一组全是新问题" | ❌ **撤回**，阈值假象 |

**因此改为以连续量 `residual_similarity`（相似度中位数）为首选指标**——
它不依赖阈值。二分类的 `restatement_rate` 保留但标注为阈值敏感，
并提供 `restatement_sensitivity` 让人自己看结论稳不稳。

> 这一节本身是个例子：**指标通过校准只说明它能分开校准集里的情形，
> 不说明它在真实数据的分布上有分辨力。** 两者之间还隔着一次分布检查
> 和一次敏感性分析。

### 14.8 残差分数的计算链路与它的混淆因素

**能不能把一个分数的来路讲清楚，是它能不能当依据的前提。** 把链路摊开：

```
① 模型输出文本
② 解析 stance_on[target].residuals          ← 条目粒度由模型自己决定
③ 写入 stance.emit（原始输出同时归档到 turns/）
④ evaluate 读成 {源 → 目标: [条目]}
⑤ residual_flow：字符串精确比对算 未再列 / 新增 / 结存
⑥ 每条新增残差与上一轮同向残差求余弦，取 max   ← 候选越多 max 越高
⑦ 取全部 max 的中位数 = residual_similarity
```

第 ② 步与第 ⑥ 步各有一个混淆因素，**两者同向**，实测后果严重：

| 混淆 | 机制 | 实测 |
|---|---|---|
| 条目粒度 | 模型自选写几条、每条多长；文本越长共享词越多，余弦越高 | 与相似度相关 **r=+0.858** |
| 候选数 | 候选越多，`max()` 越容易撞上高分 | 与相似度相关 **r=+0.830**；单对残差实测从 0.605 抬到 0.763 |

实际数据完全被它解释：

| 场次 | 每轮条数 | 条目均长 | 相似度 |
|---|---|---|---|
| claude ×2 | 13.5 / 15.0 | 148 / 154 | 0.741 / 0.652 |
| DeepSeek ×2 | 5.5–6.5 | 54–65 | 0.524–0.606 |

> **因此 14.7 中"仍然站得住"的那条结论也撤回。**
> "claude 场比 DeepSeek 场更像在改头换面"——那不是辩论行为的差异，
> 是**文体的差异**：claude 每条写 150 字、DeepSeek 写 57 字。
> 至此，语义比对得出的跨配置结论**全部撤回**。

#### 第 ⑤ 步：字符串精确比对使三个指标退化为常数

条目按**字符串精确相等**比对。而 LLM 几乎从不逐字重复自己——
**实测 51 条残差中，与上一轮逐字相同的是 0 条。**

后果是三个指标根本没在测量：

| 指标 | 实际含义 |
|---|---|
| `residuals_dropped`（未再列） | 恒等于**上一轮全部条数** |
| 新增 | 恒等于**本轮全部条数** |
| `residual_turnover`（换手率） | 只反映条数变化；六场全在 0.89–1.00 |

**换手率从头到尾没有测量任何关于内容的东西**，而它曾被用来在
`sesa eval` 里发预警、写进本文档、当作分析依据。三者已删除，
`residual_flow` 改为只报条数，并新增 `residual_trend`（条数净变化）。

> 字符层只负责挑出候选，判断内容有没有推进本来就该交给语义层。
> 把字符层的输出当成内容层的结论，是这一路最隐蔽的一次错误——
> 因为数字看上去一直在变，像是在测什么。

#### 还剩什么能用

场**内**逐轮的粒度稳定得多（长度变异系数中位 0.104，跨场约 0.48），
因此**同一场内部的跨轮趋势仍然可测**——但仍需检查该场自身的变异系数。

因此 `residual_similarity` 被约束为：

- 必须与 `residual_granularity`（条数 × 均长 × 变异系数）**并排呈现**
- 跨场比较前必须通过 `comparable_with()`（粒度差 ≤25%）
- `sesa eval` 在粒度不匹配时**主动拒绝给出跨场结论**，
  在场内变异系数 >0.25 时打星号警示

> 把混淆因素做成工具强制暴露的东西，而不是留在注释里——
> 因为写注释的人和读数字的人往往是同一个，而他会忘。

### 14.9 一次推理错误：不该用「不设裁判」去回避评测判官

本项目为回避"引入裁判"，先后造了 **6 个计数与嵌入的代理指标**，
全部失败（§14.5、§14.8）。但那条原则被用错了地方：

| | 议事中的裁判 | 评测中的判官 |
|---|---|---|
| 读什么 | 进行中的讨论 | **已结束**的转录 |
| 影响结论吗 | **会**——它决定谁对谁错 | **不会**——议事早已结束 |
| 本项目的立场 | 不设 | 应当设 |

**把针对前者的原则套用到后者，是一次推理错误。** 而反例一直摆在眼前：
一场议事中 Claude 读我们落盘的事件流、指出 `min_confidence` 的缺陷——
那正是对议事内容的语义理解，而且有效。

#### 判官自身的失效模式，逐一防住

| 失效 | 防护 | 实现 |
|---|---|---|
| **幻觉引用** | 每条判断必须附原文摘引，**机械核验**是否真在转录里；核验不过即作废 | `verify_quote`，忽略空白与标点，短于 8 字不予采信 |
| **自我偏好** | 判官不得是该场参与者 | `assert_not_participant` |
| **不稳定** | `--repeat` 多次评判并报一致率，低于 0.7 提示不宜采信 | `agreement` |
| **过度归因** | prompt 显式给出"仅扩充论证"档并声明它是默认 | 三档判定 |

#### 首次真实评判的结果

同一场议事（DeepSeek ×2，判官用 Claude）：

```
ds-conservative  仅扩充论证   三轮里结论始终是「否决 A/C、D 为主干、B 为辅助」，排序与取舍未变
ds-radical       实质改变     首轮把 B 定为主干、D 只作配套；r01 已改口为「采用 D 为主，且拒绝 B」
```

**一个人真的改了，另一个只是在扩充论证**——这个区分，字符比对、嵌入、
换手率折腾了整整六个指标都做不出来，判官一次就给出了，且摘引经核验属实。

> 教训不是"该早点用判官"，而是：**当一条原则让你绕远路时，
> 先确认它针对的是不是眼前这件事。**

### 14.10 判官层的第一次交叉评判，以及一个实验设计缺陷

六场议事交叉评判（claude 的场用 DeepSeek 判、DeepSeek 的场用 claude 判），
14 条判断：

| 判定 | 通过 | 作废 | 通过率 |
|---|---|---|---|
| 仅扩充论证 | 3 | 1 | 75% |
| 实质改变 | 6 | 4 | 60% |

看起来「实质改变」更容易被作废——但拆开按判官看，主因不是判定类型：

| 判官 | 通过率 | 判「实质改变」 |
|---|---|---|
| claude | **88%** | 6/7 通过 |
| ds | **33%** | **0/3 通过** |

**判官可信度的差异（88% vs 33%）远大于判定类型的差异。**
没有摘引核验这道机械关卡，两个判官的输出看起来会一样可信。

#### 设计缺陷：判官与被判场次完全绑定

claude 只判了 DeepSeek 的场，ds 只判了 claude 的场。因此分不出
「claude 是更好的判官」与「DeepSeek 的场更好判」。

更要紧的是**判定分布也被污染**：6 个「实质改变」全来自 claude 判 ds 场，
3 个「仅扩充论证」多来自 ds 判 claude 场。因此**不能**据此说
「多轮辩论在 2/3 的情况下产生实质改变」——那个数字是混淆的产物。

> 这与前面六个指标的错误是**同一类，换了个层面**：
> 前者是指标测的不是它名字所指的东西，后者是实验设计让两个变量无法分离。
> 共同点是——**数字本身看起来完全正常。**

修法是引入中立第三方判官评判全部场次，让判官这一变量恒定。

### 14.11 文献对照：三条与本项目做法相悖的证据

调研 2024–2026 的多智能体辩论工作后，有三条**直接指向本项目当下的方法**。

#### 相悖一：同判官重复几乎不构成可靠性证据

[When the Judge Changes, So Does the Measurement (2026-07)](https://arxiv.org/html/2607.08535v1)
实测**同质陪审团的误差相关性 ρ = 0.944–0.972**，因此"陪审团规模的影响
远小于误差依赖性"——多加同款判官几乎不带来增益。

本项目此前用 `--repeat 3` 得到"一致率 100%"并当作判官可靠的证据，
**那只度量了确定性**：一起错也会一起"一致"。
已改：`--repeat` 明确标注为确定性检查，新增 `cross_agreement`
（跨模型交叉判定）作为可靠性证据。
后续的跨模型交叉判定得到 **5/5 完全一致**，那才是有效证据。

#### 相悖二：缺少「无发言人」对照组——这是最根本的缺口

两项独立工作给出同一结论：

- [Not All Flips Are Conformity (2026-06)](https://arxiv.org/abs/2606.00820)：
  MMLU-Pro 主设置下，**仅靠自我反思就有 37% 的观测发生变化**。
- [Most LLM Conformity Needs No Speaker (2026-07)](https://arxiv.org/pdf/2607.05545)：
  加入 speaker-free floor 控制后，大部分被测到的"从众"依然存在——
  现有基准**系统性地把变化过度归因于社会影响**。

**因此"辩论使 X% 的参与者改变了立场"这句话，在没有对照组时不成立。**
本项目用 `sesa judge` 测出的实质改变，此前无法排除"不辩论也会变"。

已加入 `reflect` 协议作为对照基线：同样的参与者、同样的轮数，
但每人只看自己的上一轮、互不可见。**只有超出该基线的变化才能归因于辩论。**

> 这与前面六个指标的错误同形，但更根本：
> 那些是"测的不是名字所指的东西"，这条是"测对了，却归因错了"。

#### 相悖三：变化多不等于辩论有价值，甚至可能相反

同一篇 2026-06 的工作：严格从众造成的翻转中 **57–77% 是由对变错**；
且**空洞的推理表述也能在"抗性 agent"中诱发 20–39% 的错误采纳**——
"像在讲道理"本身就有说服力。原文明确指出：**无 ground truth 时，
有益影响与有害影响无法区分。**

本项目此前隐含地把"立场漂移大"当作正面信号。**两重错误**：该指标本身
后来被校准推翻（它测的是措辞不是立场，已失效），而即便它有效，
"变化多"也不等于"变得好"。已在 `sesa judge` 的输出里固定声明：
**只回答"变没变"，不回答"变得好不好"。**

#### 文献支持本项目的部分

| 本项目的做法 | 文献支持 |
|---|---|
| 共识不能作为终止条件 | [Wald-SPRT (2026-05)](https://arxiv.org/html/2605.19193v1)：共识早停"根本上不安全"，实测 87.7–97.8% 样本在末轮前终止，而其准确率在 67.9–94.9% 间大幅波动，共识**不提供覆盖保证** |
| 机械核验摘引而非信任判官自述 | LLM 自查引文准确率仅约 38%，低于随机（[2606.21155](https://arxiv.org/html/2606.21155v2)） |
| 词面/嵌入代理测不出语义立场变化 | 立场动力学工作明确不依赖 lexical similarity（[DEBATE 2025-10](https://arxiv.org/abs/2510.25110)） |
| 判官不得是参与者 | 自我偏好是公认的判官系统性误差 |
| 定位区别于任务分派型编排器 | [awslabs/cli-agent-orchestrator](https://github.com/awslabs/cli-agent-orchestrator)、Conductor 等**全部是主管→工人的任务分派，无一做辩论/共识与分歧报告** |

#### 未找到外部佐证的两处自有观察

1. **判官在"实质改变"类判定上的作废率高于"仅扩充论证"**——
   未找到针对这一对标签的专门研究。已知的只是判官普遍存在保守偏差。
2. **用词面/嵌入代理测语义立场变化然后系统性翻车的完整记录**——
   公开文献里没有对应物。本项目 §14.5、§14.8 的六次失败可能是原创的负面经验。

> 这两处没有外部佐证，因此**更需要自证**，不能当成已确立的结论引用。

### 14.12 debate vs reflect 对照：成本侧的确定结论

同议题、同两位参与者、同角色、同 3 轮，**唯一变量是 `protocol`**
（每组 n=3，DeepSeek ×2）：

| 指标 | debate | reflect | 差异 |
|---|---|---|---|
| 每次发言字数 | 1841 | 2029 | −9%（噪声内） |
| **input tokens** | **9994** | **8311** | **+20%** |
| **output tokens** | **9039** | **7004** | **+29%** |
| 墙钟 | 68s | 60s | +13% |
| 终局 | 2× 有保留共识、1× 假共识 | 3× exhausted（无立场卡故必然） |

**辩论比"各自再想一遍"多花约 20% 输入、29% 输出 token**，
而发言长度几乎相同——多出来的成本来自把他人发言喂进上下文，不是写得更长。

这部分不依赖判官，是机械可确认的。**问题只剩：多花的这些换来了什么。**
该问题需要判官评判两组的立场变化率，且**只有超出 reflect 基线的部分
才能归因于辩论**——文献显示仅靠自省就有约 37% 的变化。

> 注意 `reflect` 组恒为 `exhausted`：无发言人就无法对他人表态，
> 立场卡永远为空，共识判定在结构上不可能收敛。这是对照组的正常表现，
> 不是失败——该组的价值在于提供"不辩论时会变多少"的基线，
> 不在于它自己能不能达成共识。

### 14.13 判官侧：第一个有对照的存在性证据

同一判官（Claude，两组都不是参与者）评判两组：

| 组 | 判断数 | 摘引核验通过 | 实质改变 | 仅扩充论证 | **实质改变率** |
|---|---|---|---|---|---|
| debate | 6 | 6/6 | **2** | 4 | **33%** |
| reflect | 6 | 5/6 | **0** | 5 | **0%** |

**辩论组出现了实质立场改变，自省组一次都没有。**
这是本项目第一次拿到**有对照**的证据支持"多轮辩论改变立场"——
此前所有说法都无法排除"不辩论也会变"。

一个值得注意的细节：**两组里改变的都是 `ds-radical`，
`ds-conservative` 六次全部是"仅扩充论证"。**
保守派人设从不改口；激进派只在**能看到对方**时才改。
这与 §14.4 的观察一致——role 可能让参与者更不肯认输——
但那里是异模型 n=8 的弱信号，这里是同模型对照下的直接观察。

#### 这个结果的局限，必须一并写明

1. **n=3 每组，6 条判断。** 33% 与 0% 的差距看着干净，
   但绝对量是 2 条 vs 0 条。再跑几场完全可能变。
2. **单一议题、单一模型对（DeepSeek ×2）、单一判官。**
   §14.11 引用的文献给出的自省基线是约 37%，我们测到 0%——
   差距如此之大，更可能说明**我们的议题或参与者设置特殊**，
   而不是文献错了。
3. **"改变"不等于"变好"。** 见 §14.11 相悖三：
   无 ground truth 时有益与有害影响无法区分。
   本表**只说明辩论产生了自省不产生的变化，不说明那些变化是改进**。
4. reflect 组有 1 条判断因摘引无法核验而作废，样本进一步缩小。

> 结论应当这样表述：**在本设置下，辩论产生了自省基线之外的立场改变。**
> 不能表述为"多轮辩论有效"——那需要更多议题、更多模型对，
> 以及一个能判断"变得更好还是更差"的手段。

### 14.14 代码任务：第一个有 ground truth 的对照，结果是零结果

本项目此前每个结论都卡在同一句话上——**"改变 ≠ 变好，无 ground truth 时
二者无法区分"**。代码任务自带 ground truth，因此重跑了 debate vs reflect。

**设计**：一道 `parse_duration` 解析题，SPEC 明列 6 种必须抛 `ValueError`
的错误情形。参与者拿到规格与空实现，自己写实现与测试；
**评分用一份他们从未见过的留出测试**（11 条，逐条对应 SPEC）。
两组唯一变量是 `protocol`，参与者均为 `claude` CLI ×2（同模型、对立 role）。

| | debate | reflect |
|---|---|---|
| **留出测试** | alice 11/11 · bob 11/11 | alice 11/11 · bob 11/11 |
| 发言字数 | 11264 | 9610（+17%） |
| 耗时 | 679s | 592s（+15%） |
| 交叉测试 | 双向失败 | 单向失败 |

**在这个任务上，辩论没有带来任何正确性收益，只多花了 17% 字数与 15% 时间。**

#### 这是出题的问题，不是结论

任务太简单——**单个 agent 一轮就满分，天花板效应下任何方法都测不出差异**。
这个零结果无法支持"辩论无用"，只能支持一条更有用的产品建议：

> **一个所有人都能满分的任务，不值得开议会。**

已把它做进工具：`sesa eval` 检测到各方**自测**全绿时主动提示考虑换
`ensemble` 或把题出难一点。注意只看自测——**交叉测试失败恰恰是有价值的
信号**（说明各方的实现或测试存在真实分歧），算进"太容易"的判断是错的。

#### 与上一节的张力，恰好印证了文献

§14.13 在文本议题上测到"debate 实质改变 2/6、reflect 0/6"，看着是辩论有效。
这里有了 ground truth 才看得出：**辩论确实让人改了立场，改完之后正确性一样。**

这正是文献那句话的实证——**无 ground truth 时，有益影响与有害影响无法区分**
（§14.11 相悖三）。有了 ground truth 才知道：这次的改变既没变好，也没变坏。

#### 一个旁证

两个 agent 都主动处理了 SPEC **没提到**的坑：`\d` 在 Python 里是 Unicode
感知的，会匹配全角 `１`，而 `int()` 照单全收，导致 `１h` 被静默解析成 3600。
两人都改用 `[0-9]` 把语法钉死在 ASCII 上——这是超出规格的正确性判断，
而留出测试并未考察它。**任务的真实难度可能比留出测试测到的更高，
只是两者都轻松越过了。**

### 14.17 一次实验作废：缺失的上下文伪装成模型的判断

弱模型对照（DeepSeek ×2 跑 semver 题）第一次的结果是
debate 组 alice 28/34、bob 21/34，看起来终于打破了天花板效应。
**这两个数字全部作废。**

DeepSeek 走 API，写不了文件——引擎为此加了"从文本提取代码块并落盘"。
但**"读不了文件"这另一半我漏了**：交给参与者的只有 `task.md`
（一句"按仓库里的 SPEC.md 实现"），SPEC 本身从未进入上下文。

参与者自己把这件事说破了，而我当时没读到：

> 「我注意到一个关键问题：**bob 和我都没有看到 SPEC.md 的实际内容**。」
> ——alice，第 1 轮

bob 据此写下 `# NOTE: This parser intentionally does NOT support ^, ~,
x, or hyphen ranges.`。我一度把它读成「模型明知规格却拒绝实现」，
并准备当作"辩论未能纠正公然违规者"的证据报出去。
实际上它**不知道规格要求什么**。

这是 14.5 那个错误的第四次，形态换了但同源：前三次是
**一个空指标顶着满意的名字**，这次是**一个缺失的输入顶着模型判断的名字**。
共同点仍然是：我先按名字读了它，而没有先问"这个数字的输入是什么"。

修复：写不了文件的参与者，工作目录的内容随提示一并注入
（`patch.render_workspace`，带回归测试）。
修复后同一道题重跑，两人都开始逐条讨论规格里的细则条款。

> 这一条同时也是**对"参与者说了什么"要机械检查的又一个理由**。
> alice 那句话就在转录里，第 1 轮第一段，我读了那份转录却没看见它——
> 因为我在找"谁的实现更好"，不在找"输入对不对"。

### 14.18 辩论的第一个可见机制：整段照搬对手的代码

修好 14.17 那两个缺陷之后重跑，DeepSeek ×2 × semver × **24 场**（三批）。

#### 分数：辩论没有让代码更正确

| | 可评分格子 | 变好 | 变差 | 净分 |
|---|---|---|---|---|
| debate | 16 | 4 | 4 | **−3** |
| reflect | 16 | 1 | 0 | **+1** |

组间 Fisher **p=0.101，不显著**。这个 n 分辨不出协议效应，
唯一能说的是：**辩论没有表现出优势**。

#### 机制：越过 0.5 相似度线的三次，全部分数下降

三次倒退带同一个签名——改后的代码不再像自己的初稿，却很像对手的初稿：

| | 像自己 r0 | 像对手 r0 | Δ |
|---|---|---|---|
| debate-7/bob | 0.10 | 0.75 | −1 |
| debate-8/alice | 0.08 | **0.97** | −11 |
| debate-12/alice | 0.17 | **0.95** | −11 |
| 未越线的 13 格 | 0.53–0.97 | 0.00–0.43 | 倒退仅 1 次 |

`debate-8/alice` 是最干净的一例：它第 0 轮写出 34/34 的正确实现，
对手 bob 是 23/34。辩论一轮后 alice 交出的代码与 bob 的初稿相似度 **0.97**，
与自己的初稿只有 0.08，连 `ValueError: 非法版本号: '='` 这个报错都原样继承。

`debate-12` 更像一次天然对照——两人在同一场里对调了：

```
alice  34 → 23   像自己 0.17  像对手 0.95   照搬对手，崩了
bob    23 → 34   像自己 0.84  像对手 0.14   守住自己去修，修好了
```

**三次照搬的对象，分数都不高于照搬者本人。** 没有一次是向更好的一方靠拢——
与文献「严格从众的翻转中 57–77% 是由对变错」方向一致。

#### 这个结论的强度，说准

- 组内 Fisher p=0.007，但**含事后发现的两批**，不是确认性证据
- 第三批是**预注册后跑的**（判据、阈值、证伪条件先写死），
  主预测方向成立，但照搬组 **n=1**，属于极弱确认
- reflect 组零倒退**不构成独立证据**：互不可见，照搬在结构上就不可能发生
  （该组「像对手」最高只有 0.16），它只是这条机制的缺席
- 大量格子停在 34/34 的天花板，进一步压低了功效

#### 阈值 0.5 是怎么定的

reflect 组互不可见，两份独立初稿之间的相似度实测 0.03–0.16，
那是同一模型在同一提示下的天然重合。0.5 远在这条基线之上。
**只在 DeepSeek × semver 这一种情形下校准过**，换模型换任务需重新校准。

#### 已做进工具

`sesa eval` 现在报告照搬事件（`evaluate.code_adoption`）。三条纪律：

1. **只报事实，不判好坏**——好坏看执行证据有没有变好，不看这个数
2. **「测不了」与「测了没发现」分开**（`AdoptionReport.measurable`）——
   agent CLI 自己写文件，代码不进发言正文，那种场次根本没法测
3. **「手里没有东西拿了对方的」不算照搬**——实测中前者 4 次、后者 3 次，
   只有后者伴随分数下降；混成一类会让检出数从 3 涨到 7，多出的 4 次不含信号

> 这条对 Sesa 的意义是直接的：**辩论的风险不是不收敛，是收敛到错的那一方。**

#### 已接进引擎（不再只是事后指标）

议事进行中，每轮证据跑完之后引擎会做一次照搬检测，读的是**工作副本本身**
而不是发言正文里的代码块——这样自己写文件的 agent CLI 同样能测。检出的
事件带上照搬前后的自测退出码，于是「收敛到对方」与「收敛到对的答案」
第一次可以分开：

| 情形 | 引擎的处理 |
|---|---|
| 照搬 + 自测由通过转失败 | 终局由 `consensus` **降级为 `consensus_with_reservations`**（退出码 0→3），`RESULT.md` 在结论**之前**顶一条警告，并指出被放弃的那份实现还在哪个分支 |
| 照搬，但证据没变差 | 只陈述事实，不判好坏——也可能是真被说服了 |
| 照搬，但该场没有执行证据 | **不降级**。相似度不回答好坏，没有证据就不下判断 |
| 上一轮自己没有这个文件 | 不算照搬。手里本来没东西拿了对方的，与抛弃自己的成果性质不同 |

降级只在有执行证据时发生，这是刻意的：本项目已经四次栽在「空值伪装成
数据」上，不能再让一个相似度数字冒充正确性判断。

### 14.19 异构辩论：产品的核心前提，第一次被测，未获支持

此前所有带 ground truth 的正确性实验，**参与者都是同一个模型两份**——
而本项目的核心前提是"**不同** agent + **不同** LLM 的组合"。
换句话说：那些负面结论测的都不是产品的前提。

配置：`claude`（cli adapter，自己写文件）+ `deepseek`（openai_compat，
引擎代为落盘），**adapter 与模型双重异构**，两人 role 相同。
任务是 node-semver 全文法 + 范围集合运算，118 项留出测试。每组 4 场。

#### 出题过程本身是一次教训

| 题 | 留出测试 | claude 单跑 |
|---|---|---|
| 旧 semver | 34 项 | 4×34/34 |
| 全文法 semver | 85 项 | 85/85 |
| + 范围求交/包含 | 118 项 | **118/118 × 8 场** |

三次独立加难，三次满分。**继续加难度就是任务层的 p-hacking**——
一直换题直到出现想要的结果。到此为止：

> **强模型 + 规格明确的任务 = 没有可改进的空间。**
> 辩论在这里不可能有增益，因为没有空间可增。

第二道题上 claude 还挑出了**出题人的错误**：`subset('~1.2.3', '1.2.3 - 1.2.99')`
我的穷举 oracle 判 True，它判 False。反例是 `1.2.100`——我的两个校验全集
patch 只到 6 和 9，**以同一种方式漏掉了同一个反例**，"两个全集比对"这道
自检因此形同虚设。已改为由表达式里的字面量推导全集。
（这是本轮第二次由被测者发现出题人的错误，上一次见 14.17。）

#### 结果

| | n | r0 均值 | 各场 Δ | 均值 Δ |
|---|---|---|---|---|
| debate | 4 | 81.0 | +5, 0, +24, +19 | **+12.0** |
| reflect | 3 | 70.3 | +19, +44, +6 | **+23.0** |

- **主预测（弱者在辩论组涨得更多）不成立，方向相反。** 但 n=4 vs 3、
  Δ 散布 0 到 +44，且 reflect 起点低 10.7 分（均值回归偏向它）——
  **只够说主预测没得到支持，不够说反向成立**
- **次预测（强者不倒退）成立但无信息量**：claude 8 场全 118/118，
  它本就在天花板上，且逐轮代码当时未落盘，无法排除中途起落
- **机制预测无从检验**：照搬 0 次（同质组 32 次里也只出现 3 次，本就稀有）

#### 这一轮暴露的四个自身缺陷

| 缺陷 | 后果 |
|---|---|
| 逐轮工作副本算完就扔 | 自己写文件的 agent CLI 中间成果拿不回来，次预测只能看终态 |
| 截断修复过度矫正 | 从"静默接受"荡到"整份丢弃"，4 个 turn 的可用代码被扔掉，其中一个已写出 109/118 |
| 子进程失败只报 stderr | claude 的 `You've hit your session limit` 只写在 stdout，8 场因此全部报废而看不出原因 |
| `reflect` 看不到自己的执行证据 | 对照组测的成了"什么都没有"而非"没有同伴" |

第四条方向值得注意：修复前辩论组的信息**严格多于**自省组（多了对方主张、
双方退出码、还有自己的测试结果），**拿着更多信息仍然没赢**。
但严格说，修复后的对照数字必须重跑才能给出，旧数字不能冒充新对照下的结果。

### 14.20 用 Sesa 验 Sesa：一次闭环，八个真缺陷

议题：**README 那四条设计底线，代码真的做到了吗？** 参与者拿到本仓库的
完整源码（各自一个 git worktree，可读可改可跑测试），要求每条发现必须给出
`文件:行号` + 失败场景 + 核验方式，并明写「**报不出问题是合格的答案，
编一个出来不是**」。

结果：claude 写出 10 条测试，其中 **8 条在当时的代码上失败**，
逐条机械核验**全部为真**。

| 底线 | 缺陷 |
|---|---|
| —— | **`sesa run` 在真实终端里必崩**：`ConsensusReport(unresolved=…)` 传的是 property 不是字段 |
| 2 | 零个 agree 格子仍被判为 `partial_coverage_consensus`——覆盖不全的「共识」也得先有共识 |
| 2 | 空矩阵（单人／无人表态）没有 blocker ⇒ `converged=True` |
| 2 | 终局说「覆盖不全」，交付的 JSON 却写 `coverage=1.0` |
| 2 | `reconcile` 把 `unresolved`（反对＋未测到）整体说成「明确反对」 |
| 3 | **残差换个措辞就能让僵局检测永不触发** |
| 交付层 | `partial_coverage_consensus` / `not_measured` 没配横幅，退化成裸枚举值 |
| 交付层 | `not_measured` 被写成「本场议事并未达成共识」 |

#### 最难堪的两条

**一、`sesa run` 在终端里必崩。** 这条路径只在 TTY 下走；管道与 `--json`
都绕开它。而我此前所有手工验证都把输出重定向到文件，264 个测试也全在
非 TTY 下跑——**于是新用户第一步就会撞上的路径，一次都没被执行过**。
我在此前一条消息里刚汇报完「干净环境装得上、跑得通、端到端验证通过」。

**二、残差换措辞可无限期推迟僵局。** `_stalled_rounds` 的注释明写着
「自述『我改变了立场』**不足以**清零停滞计数器……否则一个爱说自己改了的
参与者就能把僵局检测无限期推后」，紧接着调用的 `_residuals_changed`
docstring 却写着「**不依赖自述的客观信号**」——**而残差本身就是自述文本**。
正门堵住了自述，侧门又把它放了回来，还给它起名叫「客观」。
这是 14.5 那个命名错误的又一次：**给一个东西起名叫「客观」，不会让它变客观。**

实测对照（双方立场每轮完全相同，唯一变量是措辞）：
不换措辞 → 第 2 轮判 `deadlock`；每轮换措辞 → 连续 5 轮 `stalled` 恒为 0。
现在只认**条数变化**，并在 docstring 里写明这仍可被拆句子刷、成本更高而已，
不假装它无懈可击。

#### 关于这次闭环本身

- **claude 那一轮最终标记为失败**（撞上会话额度），但它的工作副本已提交到
  分支上——**产出留下来了，发言没留下**。这些测试是从失败的那一轮里捞出来的。
- 因此这次**没有真正形成两方辩论**，实际是「claude 单方评审 + deepseek 单方
  评审」。协议层面的价值这次没测到。
- deepseek 那侧拿到了 `briefing`（外部工具 open-code-review 的 18 条扫描结果，
  未经筛选，并明确告知「它可能有误报，你的任务是逐条核实」）。
  **⚠️ 这个配置不真实**：真实用法里扫描报告会给所有人（见 14.22）。
  本节里凡是关于"辩论动力学"的观察都因此存疑；**缺陷本身不受影响**，
  它们是逐条在代码上核验的。
- 更早一轮里，同一个外部工具还独立找出 6 个真缺陷（版本号走散、
  终局定得比执笔晚 70 行、转义漏网两处等），同样全部核验为真。

> **自审有系统性盲区，而且盲区不随「更仔细」消失。**
> 这一轮的 14 个真缺陷全部出自我当天写的代码，我自己复查过多遍，一个都没看见。
> 换一双眼睛——哪怕是更弱的模型配一个外部工具——找出来了。

### 14.21 四轮闭环：产出曲线没有收敛，以及为什么

用 Sesa 审 Sesa，议题固定为「README 那四条底线，代码真的做到了吗」，
每条发现必须给出 `文件:行号` + 失败场景 + 核验方式。四轮的真缺陷数：

| 轮次 | 扫描目标 | 真缺陷 |
|---|---|---|
| 1 | 引擎与共识核心 | 8（另有外部工具独立找出 6） |
| 2 | 交付层 | 14 |
| 3 | 类型 / 配置 / 适配器 / 工作区 | 9 |
| 4 | **重扫第 1 轮的同一批模块** | **18** |

第 4 轮是专门设计来回答「见底没有」的。**答案是没有，而且产出还涨了。**

#### 但这条曲线有个混淆，必须说清楚

前三轮扫的是不同模块，测的是「新地盘还有多少雷」，不是「同一块地盘变干净了」。
第 4 轮虽然重扫，但那批模块在前三轮里被大改过——所以它测的也不全是残留，
**相当一部分是修复过程中新引入的**。

后者更值得警惕。第 4 轮里至少两条是当天的修复亲手造出来的：

> 为了修一个单调性倒挂（「更弱的一致换来更好的终局」），我给置信度门槛
> 加了 `if report.min_confidence and ...`。**而 0.0 是假值**——于是
> 置信度 0.00 判成「有保留的共识」、0.01 反而判成「未完成」。
> **同一个函数、一小时内、同一形态的第二个倒挂。**

根因是 `0.0` 兼任「没报告」的哨兵与「我很没把握」的合法取值。
这是 14.5 那个错误的又一次：**一个位置承担两种含义，迟早要出事。**

#### 一个根因，四条症状

`find_json_blocks` 的 docstring 写着「代码块在前、裸对象在后」，
而 `parse_stance` 靠「从后往前取第一个」定位末尾的立场卡——两个假设互相矛盾。
后果：发言里引用别人的立场卡时（讨论分歧时很常见），被引用的那张会盖掉
作者自己的；`strip` 与 `parse` 选中不同的块；判官的总评被它复述的 JSON 顶掉。

**找散落的同源错误，比找孤立的 bug 有价值得多。** 同类的还有
「把没测到说成分歧」，它在四个出口各犯一次，每修一处就从下一处冒出来。

#### 这一切说明什么

1. **自审有系统性盲区，且盲区不随「更仔细」消失。** 49 个缺陷没有一个是
   作者复查发现的——而每一处代码都复查过多遍。
2. **修复本身是新缺陷的主要来源。** 这不是「代码质量差」，是「改动即风险」
   的常态；区别只在于有没有一双能当场看见它的眼睛。
3. **不照单全收。** 第 4 轮有一条要求把参与者引用的别人的立场卡也切掉——
   那是在删他的论据，已否掉并写明理由。**结论是核验出来的，不是投票出来的。**

#### 换个打法之后，曲线终于下来了

前四轮扫的是整个模块。第五轮改成**审刚写的 diff**——因为曲线已经显示
新缺陷主要来自修复本身。同一批 diff 连审三遍：

| 遍 | 报出 | 为真 | 最高严重度 |
|---|---|---|---|
| 一 | 12 | 11 | high |
| 二 | 4 | 4 | medium |
| 三 | 1 | 1 | medium |

**审「刚改的东西」比审「整个模块」划算得多**：命中率从大约一半升到近乎全中，
而且能看到真正的收敛。前四轮那条不降反升的曲线，很大程度是因为每轮换了地盘。

#### 三次「修复引入新缺陷」，形态完全一样

| 我修的 | 我造出的 |
|---|---|
| 单调性倒挂（更弱的一致换来更好的终局） | 用 `if x and` 判 0.0，造出第二个倒挂 |
| 「查不了」与「没问题」要分开 | 两种情形都 `return []`，而 docstring 声称区分了 |
| 两条抽取路径判得不一样 | 让其中一条把「同意的理由」判成「保留意见」，凭空制造分歧 |

**每一次都是把要防的那个错换个方向又犯一遍**，每一次都由下一轮闭环当场逮住。
第三条尤其值得记：原来的缺陷是漏判，我的修复把它变成了错判——
**错判比漏判更糟**，漏判还有 default-deny 的其他关卡兜着，错判是无中生有。

> 这套方法已经打包成 `examples/self-review/`，任何项目都能直接用。
> 它同时是本项目**唯一覆盖「真跑一整场」的测试**——TTY 渲染崩溃那类缺陷
> 在 pytest 里永远看不到，因为测试跑在非终端下。

### 14.22 第四条方法论纪律：配置必须是产品会自然产生的

前三条纪律（14.5、14.7、14.11）防的都是"测量方法错了"。这一条更基础，
防的是"**测的根本不是会发生的事**"：

> 用户发起议事时给的是**一个**任务，所有参与者都看得到它。
> 手上有一份扫描报告、一份 RFC、一份日志导出，**没有任何理由只给
> 其中一个参与者看**——要给就都给。

自审那四轮里，我给 deepseek 挂了 `briefing`（每人私有的材料）、claude 没有，
理由是"制造信息不对称让分歧有信息量"。**那是为做实验造出来的场景。**
更糟的是我把同一个配置写进了 `examples/self-review`——
一个示例若示范了用户不会有的用法，它教给别人的就是错的。

#### 受影响的界限，说清楚

| 结论 | 是否受影响 |
|---|---|
| 那些缺陷本身 | **不受影响**。逐条写成失败的测试、跑一遍、看它红——与谁看到什么材料无关 |
| "辩论产生的是新的注意力分配"（14.20） | **受影响，已降级为存疑**。那条链子（deepseek 转述工具的发现 → claude 顺着去挖）**完全依赖于只有一方看得到扫描结果**。真实配置下两人都看得到，那次传递根本不会发生 |
| semver-weak 24 场、异构 8 场 | **不受影响**，两组材料本来就对称。异构那组给 deepseek 注入工作目录，是因为它读不了文件——那是**把两边拉平**，方向相反 |

#### 已做的处置

- `examples/self-review` 改用 `--file`：扫描结果与议题合并，所有人可见
- 测试从「briefing 只给一位」改成「**示例不得示范私有材料**」
- `briefing` 保留但降级为例外通道，docstring 给出判据：
  **这份材料为什么不能给别人看？答不上来就走 `--file`。**

> 这条纪律之所以最难守，是因为违反它的时候**实验跑得很顺**：
> 数据漂亮、结论自洽、指标也没问题。只有回头问一句
> "用户真的会这么用吗"，才看得出整件事悬空。

#### 同一天里它又犯了一次，形态是"地盘不对等"

跑"两个弱模型能不能替代一个强模型"时，我给两组的**搜索范围差了 9 倍**：

| 组 | 被扫描的代码 |
|---|---|
| claude 四轮 | 6858 行（引擎、共识、交付层这些核心模块） |
| kimi + deepseek 一轮 | **761 行**（wizard、semantic、两个小协议、两个 adapter） |

然后我拿"一轮出 1 条"去比"一轮出 8–18 条"，并写下"差距是数量级的"。
**那句话已撤回。** 让人在一间小屋里找东西，再说他不如在整栋楼里找的人
找得多——比的不是能力，是屋子大小。

任务书说的是"审这个项目"，是我在实现时把范围缩成了一小片。
**纪律要检查的不只是"用户会不会这么配"，还有"两组拿到的条件是不是对等"。**

### 14.23 发现的出处必须分清：工具、议事、还是作者

一天下来累积了大量缺陷，很容易笼统说成"多方评审找出来的"。
**那是不准确的**，而这个项目最该防的就是这种含糊：

| 出处 | 形态 | 真缺陷 |
|---|---|---|
| Claude 自审 4 轮 | **Sesa 议事**（claude + deepseek 辩论） | 8 / 14 / 9 / 18 |
| ocr 审 diff × 3 遍 | **工具单跑**（DeepSeek 驱动） | 11 / 4 / 1 |
| kimi + deepseek | **Sesa 议事** | **1** |
| ocr 全量扫 `src/` | **工具单跑**（DeepSeek 驱动） | 70 条报出，逐条核验后多数为真 |
| 作者反向普查 | 手工（受前几轮启发） | 2 类散落错误 |

最后那 70 条**完全不涉及辩论**：`ocr scan` 单跑，没有参与者、没有议事记录，
kimi 一点没参与。把它算成"kimi + deepseek 讨论出来的"会严重高估议事的产出。

#### 这也意味着一个对比至今没有答案

"两个较弱的模型 + 外部工具，能不能替代一个强模型"——

- 弱模型**议事**：1 条（761 行的地盘）
- Claude **议事**：49 条（6858 行的地盘）
- 弱模型驱动的**工具**：70 条（8642 行全量）

三个数字来自三种不同的东西，**没有一组是对等的**。第三行证明的是工具好用，
与弱模型的议事能力无关。要答清楚，得让两组在同一批**双方都没审过**的代码上
各跑一场议事——而那 70 条已经被修完了，同一批代码上再比已经不公平。

### 14.24 归因错误的一个固定形态：先读到谁，就记给谁

一天里犯了三次，形态完全一样。

| 我说的 | 事实 |
|---|---|
| 「kimi + deepseek 讨论出的 70 条」 | `ocr scan` 单跑，没有参与者、没有议事记录 |
| 「deepseek 提出了少数意见截断」 | 那条在给它的扫描材料里就有，几乎一字不差 |
| 「deepseek 指出了 R3 回归」 | **claude 第 0 轮就写下 `### R3.`**，编号都是它起的；deepseek 第 1 轮引用该编号 |

三次的共同机制：**我先读到的那份材料，决定了我把功劳记给谁。**
第三次尤其典型——deepseek 先交卷（23s vs 988s）、篇幅短、结论醒目，
我看到"R3 是最严重的回归"就直接去核验了，**从没打开 claude 那 9673 字**。

#### 怎么防

排序信息（"这条最严重"）与来源信息（"这条是谁发现的"）必须分开记账。
判断一条发现的出处，看三样东西：

1. **时间戳**——事件流里谁先交卷
2. **编号与措辞**——引用别人的编号（`R3`）说明是在附和，不是原创
3. **深度**——原创者往往能给出根因，附和者只能给出评价

> 这三样都在 `events.jsonl` 里现成躺着。**归因不该靠印象，
> 而这个项目的全部方法论就是"别靠印象"**——却在自己的产出记账上
> 连犯三次。

#### 但排序本身有价值，只是别叫错名字

deepseek 把 R3 判为"最严重"，确实让作者第一眼就看到了它——
claude 报了十条，按顺序读会先花时间在 R1、R2 上。
这是**注意力分配**，与 14.20 观察到的是同一类，
不是"弱模型超过了强模型"。

### 14.15 目前唯一可靠的观测

跨六场议事（claude ×2 两场、DeepSeek ×2 四场，含残差回灌开/关对照）：

| 观测 | 数值 | 可靠性 |
|---|---|---|
| 未决保留意见总数不随轮次下降 | 结存 6→6 / 6→7 / 5→6 / 15→12 / 15→15 | **可靠**，纯计数 |
| 第一次表态后类别判断几乎不变 | 33 场里 3 次 | **可靠**，类别值 |
| 参与者自述"我改变了立场" | 33 次 | **过报约 11 倍** |
| 残差回灌是否抑制换手 | 0.962 vs 0.909（n=2） | 无差异，样本过小 |

这些**不足以判断多轮辩论是否值得**——"总数不降"既可能是原地踏步，
也可能是讨论在变深。判断需要语义方法，而那是下一步的事。

### 14.16 仍未验证的承重论断

以下论断**尚无实验支持**，仅凭机制合理性成立。它们各自导出了具体设计，
因此都值得排进对照实验：

| 论断 | 导出的设计 | 如何验证 |
|---|---|---|
| 立场倾向是最廉价的质量增益 | Role 注入 | 有 role / 无 role 对照 |
| 轮流发言有位置偏见 | 阶段内并行 | `turn_taking: sequential` 对照，看首位发言者的立场是否被过度采纳 |
| 参与者越多分歧越有信息量 | 建议 3 人以上 | 2/3/4 人对照 |

> 观察到但尚未解释的现象：**三场议事的最终轮没有出现过一个 `agree`，全是
> `partial`。** 这意味着 `CONSENSUS` 这一档在实践中可能几乎不可达。
> 若后续数据坐实，退出码与终局分档需要重新设计。

---

## 15. 已定决策

| 决策 | 结论 | 理由 |
|---|---|---|
| 语言/分发 | Python + uv（`uvx sesa`） | 原型可演进；Textual 是最强 TUI 框架且能 `textual serve` 转 Web；跨语言嵌入走 JSONL/MCP |
| 产品形态 | 无头内核 + 三前端 | TUI 若是主体，别人嵌入时要剥渲染层；事件流才是可复用的契约 |
| 任务范围 | 文本议题与代码任务都要 | 差别仅在 Workspace + Evidence 两个接缝，其余四层共用 |
| 共识判定 | 结构化立场卡为主；解析失败则重试，再失败记 `unknown` | CLI agent 常不守格式；矩阵可计算、可解释、可复盘；不猜不代笔 |
| 终局诚实性 | 区分 consensus / deadlock / exhausted | 卡死 ≠ 统一，不粉饰 |
| **不设仲裁者** | 共识即产出；未收敛则并列交人拍板 | 避免"用未经辩论的观点覆盖辩论结果"，且省掉一个 API key 的摩擦 |
| 收敛后成文 | 参与者轮值执笔（Rapporteur），附 `drafted_by` | 分歧归零后才动笔，只整合措辞不裁定对错 |
| 参与者数量 | 用户定，最少 2 个，无上限 | 配置驱动，不预设"推荐几人" |
| 产出形态 | 骨架恒定的 `RESULT.md`，一致/分歧只改变各节比例 | 阅读方式统一；分歧必须带根因、所需信息、下一步，而非原样并列 |
| 分歧可续 | `sesa resume --inject` 从断点续辩 | deadlock 是中间态而非失败 |
| 发言编排 | 阶段串行、阶段内并行；第 0 轮强制并行 | 轮流发言有位置偏见；回应本就发生在下一轮 |
| 独任角色 | 执笔人 / 发言顺序 / 提案者，**默认一律轮值** | 不靠挑"公正的人"，靠让位置流动起来 |
| 思考可见性 | 默认不互见，可 `on_deadlock` / `always` 开放 | 理由已修正为**成本与适配器对等性**（实测 +54% token）；"防止过早趋同"这条被一次对照实验指向反面，见 14.3 |
| 证据可信度 | 分 `engine`/`claimed` 两级；交叉测试防自测；证据可反驳 | 执行结果比修辞硬，但**不是真理**——写代码的人也写测试时绿灯毫无意义 |
| 表态前提 | 只能对**读过其发言**的人表态；未表态即阻塞共识 | 否则模型凭空打分，第 0 轮就能"达成共识" |
| 假共识双向查 | 执笔人报冲突，或矩阵称一致而执笔人列出分歧 | 后者是立场卡失灵、执笔人补救的情形 |
| default-deny 计数 | 只有显式 `agree` 才算已解决 | 实测两次"双方互投 partial → 判收敛 → 执笔人读出实质分歧" |
| 共识 ≠ 终止 | 共识是报告标签，终止由轮数与预算保证 | 混为一谈会让人误以为"严格判定会使工具退化" |
| 保留意见分档 | `consensus_with_reservations` 独立成档 | 有保留不是失败，但也不该被写成完全一致 |
| 预算逐次调用生效 | 单次调用超时按剩余墙钟压缩 | 只在轮次边界检查时，900s 上限被实测冲到 1185s |
| 配置持久化 | 全局参与者库 + 项目级覆盖，凭据默认走钥匙串 | 填一次长期复用；开源项目不该教人明文存 key |

### 14.25 「评结论」与「验证据」是两件事

立场卡最初只让参与者给**结论**打分：`agree | partial | disagree`。
schema 里没有一个字段涉及对方的**证据**。后果在 20260901-103359 那场
暴露得很干净——一方这样表态：

```json
"stance_on": {"kimi": "partial"},
"premises": ["kimi 的测试文件已写入工作目录，但我的执行发生在写入之前，
              故无法亲自验证其失败；依赖其执行输出。"]
```

**它承认自己没核验，同时给出了让步的表态，而这完全合规。** 协议里没有
槽位让它说「我没查」，它只好塞进前提里；共识判定看不见这件事，是我
手工读前提才发现的。

于是把 default-deny 从「立场解析」延伸到「证据」：

> **对方拿出了引擎执行过的证据、而你一条都没核验就说同意——不算同意，
> 算没测到。**

四条不能动摇的边界：

1. **降级的去处是 `unknown`，不是 `partial`。** 降成 partial 会让未核验的
   双方拿到一枚「有保留的共识」徽章，那正是本项目反复栽的坑：把「没测到」
   当成「弱一点的同意」。宁可失败得显眼，也不要成功得可疑。

2. **门槛以「对方确实拿出了可核验的东西」为条件。** 纯设计辩论里没有可
   执行的产物，谁也验不了谁；若无条件要求核验，这类议事将**永远无法
   达成共识**——而那恰恰最需要多方参与。

3. **只有引擎执行过的证据（`is_fact`）才触发核验义务。** 参与者自述
   「我跑了，通过了」是待核验的主张；拿它当证据，等于让人靠自我宣称
   就给别人扣上核验义务。

4. **「查不了」是体面的答案，不扣分**（`how: unable` + 原因）。一旦
   「是否可验证」被计分，参与方就会为了显得合规而制造看起来可执行的
   证据，系统性压制那些本来就不该靠跑测试证明的意见（设计取舍、命名、
   可维护性）——而那是多方议事最该保住的东西。

配套：`render_evidence` 现在给出产出物的**分支名**。只给「退出码 0」
这种结论，别人无从复核；要求核验却不告诉人去哪儿查，是不讲理的。

#### 14.25.1 规则的施加点与告知点必须是同一处

`verified` 门槛落地时我先改了共识判定，**却没给参与者遵守的途径**——
提示词根本没要求这个字段。事后逐条审计"参与者会被降级"的每条路径，
同一个错误犯了**五次**：

| # | 洞 | 后果 |
|---|---|---|
| 1 | 降级重试的逐行表装不下核验 | 第一次解析失败者被换到一个**结构上无法合规**的格式 |
| 2 | `stance.emit` 不序列化 `verified` | 事件流号称唯一真相，却丢了它 |
| 3 | 续辩恢复不带 `verified` | resume 后所有 agree 失去地基，**永远无法共识** |
| 4 | 只有 debate 系渲染证据 | `adversarial` 的参与者被一条从未被告知的规则降级 |
| 5 | 门槛看本轮证据 | 本轮证据在立场卡之后才执行——**惩罚一件不可能做到的事** |

2、3 与当天上午刚修完的"截断标记丢失"（kimi 扫描 62）**是同一个病**：
新规则写在内存里，忘了它得能从事件流回来。修完一个立刻在新代码里重犯，
说明这不是疏忽，是缺一条检查纪律。

留下的纪律：

> **凡是能让参与者被降级的地方，都必须存在一条它能走通的路。**
> 施加规则的代码与告知规则的代码，要么在同一处，要么有测试钉住其配对。

因此告知移到了 `Engine._run_move` 里紧挨 `stance_instruction` 的位置——
那是**所有协议唯一的汇流处**。留在各协议模板里，每写一个新协议就重犯一次。
`tests/test_compliance_path.py` 逐条钉住这五个洞。
